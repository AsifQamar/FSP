#include <stdio.h>

int main() {
    float a = 0.5, b = 0.9;   // declare both a and b

    if (a&&b > 0.9)
        printf("sachin\n");
    else
        printf("rahul\n");

    return 0;
}
